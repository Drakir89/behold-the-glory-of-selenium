package pageobjects;

import org.openqa.selenium.WebDriver;

public class LoggedOutPage extends Page {
    public LoggedOutPage(WebDriver givenDriver){
        super(givenDriver);
        //add assert for page loaded here
    }
}
